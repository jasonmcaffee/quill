# A Quill plugin. See the JavaScript one for what the format is.
#
# CSS needed three things no earlier manifest had asked for, and `tasks/task-1671-css-plugin-tdd.md`
# records why each: a hyphen has to be a letter, or no property can be named; a third list of words,
# because a stylesheet has at-rules, properties and values and two lists could only tell apart two of
# them; and `#ff0000` has to be a number, or half the colours in a file are coloured and half are not.

plugin.id          = css
plugin.name        = CSS
plugin.version     = 1.0.0
plugin.vendor      = Quill
plugin.kind        = language
plugin.description = Syntax colouring, a file icon and the Dracula colour scheme for CSS. At-rules, element selectors, pseudo-classes and pseudo-elements, property names, value keywords, functions such as var and calc, hex colours, lengths with their units, strings and comments are all told apart.
plugin.limitations = A word that is both a property and a value is drawn the same way wherever it appears, because telling the two apart needs to know which side of the colon it is on and this reads one token at a time. Whichever reading is commoner wins: inset, left, top, right, bottom, content and border are properties, so border-style: inset is drawn as a property; flex, grid and all are values, because display: flex and transition: all are written far more often than the shorthand properties of the same name. A class or id name is plain text, because it is a name its author chose rather than one the language provides; the dot or hash in front of it is drawn as an operator. A custom property such as --brand-hue is plain text for the same reason. A hash and three, four, six or eight hexadecimal digits is a colour, so an id selector that happens to spell one - #dad, #face - is drawn as a colour. A negative length such as -8px is one word and is drawn in the plain text colour rather than as a number, because the hyphen belongs to the words either side of it far more often than it is a sign. SCSS, Sass and Less are deliberately not claimed: they take // as a line comment and CSS does not, and a grammar has one line comment.

language.extensions     = .css
language.block_comment  = /*, */
language.strings        = ", '
language.escapes        = true
language.numbers        = true
# No line comment. `//` is not CSS, and taking it as one would colour half of every url(https://...)
# as a comment.
#
# A hyphen and an at sign are letters here, so `background-color`, `--brand-hue`, `-webkit-mask` and
# `@font-face` are each one word. Neither is in the operators below, because a word is read before an
# operator is and they would never be reached.
language.word_characters = -, @
language.hex_colors      = true
language.operators       = {}();:,>+~*=/!%[]|^&.#

# The at-rules, the words that structure one, the element selectors, and the pseudo-classes and
# pseudo-elements. What they have in common is that they say where a rule applies rather than what it
# does, which is what a person scans a stylesheet for first.
language.keywords = @charset, @import, @namespace, @layer, @media, @supports, @container, @font-face, @font-feature-values, @counter-style, @keyframes, @-webkit-keyframes, @page, @property, @scope, @starting-style, @position-try, @view-transition, @swash, @ornaments, @annotation, @stylistic, @styleset, @character-variant, and, or, not, only, from, to, important, html, body, div, span, p, a, h1, h2, h3, h4, h5, h6, ul, ol, li, dl, dt, dd, table, thead, tbody, tfoot, tr, td, th, caption, colgroup, col, form, input, button, textarea, label, fieldset, legend, select, option, optgroup, img, picture, figure, figcaption, svg, header, footer, nav, main, section, article, aside, video, audio, canvas, iframe, code, blockquote, hr, br, strong, em, b, i, u, s, q, abbr, cite, kbd, samp, details, summary, dialog, template, slot, ins, del, mark, time, address, hgroup, menu, meter, ruby, wbr, noscript, script, style, head, title, meta, link, base, hover, focus, focus-visible, focus-within, active, visited, any-link, target, target-within, root, empty, checked, disabled, enabled, indeterminate, required, optional, valid, invalid, in-range, out-of-range, read-only, read-write, placeholder-shown, autofill, user-valid, user-invalid, first-child, last-child, only-child, first-of-type, last-of-type, only-of-type, nth-child, nth-last-child, nth-of-type, nth-last-of-type, nth-col, nth-last-col, is, where, has, dir, lang, host, host-context, defined, fullscreen, modal, popover-open, picture-in-picture, open, details-content, before, after, first-line, first-letter, selection, backdrop, marker, placeholder, file-selector-button, cue, cue-region, part, slotted, spelling-error, grammar-error, target-text, highlight, view-transition-group, view-transition-image-pair, view-transition-old, view-transition-new

# The property names, hyphens and all.
language.builtins = accent-color, align-content, align-items, align-self, animation, animation-delay, animation-direction, animation-duration, animation-fill-mode, animation-iteration-count, animation-name, animation-play-state, animation-timing-function, appearance, aspect-ratio, backdrop-filter, backface-visibility, background, background-attachment, background-blend-mode, background-clip, background-color, background-image, background-origin, background-position, background-repeat, background-size, block-size, border, border-block, border-bottom, border-bottom-color, border-bottom-left-radius, border-bottom-right-radius, border-bottom-style, border-bottom-width, border-collapse, border-color, border-image, border-inline, border-left, border-left-color, border-left-style, border-left-width, border-radius, border-right, border-right-color, border-right-style, border-right-width, border-spacing, border-style, border-top, border-top-color, border-top-left-radius, border-top-right-radius, border-top-style, border-top-width, border-width, bottom, box-shadow, box-sizing, break-after, break-before, break-inside, caption-side, caret-color, clear, clip-path, color, color-scheme, column-count, column-gap, column-rule, column-span, column-width, columns, contain, container, container-name, container-type, content, content-visibility, counter-increment, counter-reset, cursor, direction, display, empty-cells, fill, filter, flex-basis, flex-direction, flex-flow, flex-grow, flex-shrink, flex-wrap, float, font, font-family, font-feature-settings, font-kerning, font-optical-sizing, font-size, font-size-adjust, font-stretch, font-style, font-variant, font-variation-settings, font-weight, gap, grid-area, grid-auto-columns, grid-auto-flow, grid-auto-rows, grid-column, grid-column-end, grid-column-start, grid-row, grid-row-end, grid-row-start, grid-template, grid-template-areas, grid-template-columns, grid-template-rows, height, hyphens, image-rendering, inline-size, inset, inset-block, inset-inline, isolation, justify-content, justify-items, justify-self, left, letter-spacing, line-clamp, line-height, list-style, list-style-image, list-style-position, list-style-type, margin, margin-block, margin-bottom, margin-inline, margin-left, margin-right, margin-top, mask, mask-image, max-block-size, max-height, max-inline-size, max-width, min-block-size, min-height, min-inline-size, min-width, mix-blend-mode, object-fit, object-position, offset, opacity, order, outline, outline-color, outline-offset, outline-style, outline-width, overflow, overflow-wrap, overflow-x, overflow-y, overscroll-behavior, padding, padding-block, padding-bottom, padding-inline, padding-left, padding-right, padding-top, page-break-after, page-break-before, page-break-inside, paint-order, perspective, perspective-origin, place-content, place-items, place-self, pointer-events, position, quotes, resize, right, rotate, row-gap, scale, scroll-behavior, scroll-margin, scroll-padding, scroll-snap-align, scroll-snap-type, scrollbar-color, scrollbar-gutter, scrollbar-width, shape-outside, stroke, tab-size, table-layout, text-align, text-decoration, text-decoration-color, text-decoration-line, text-decoration-style, text-decoration-thickness, text-emphasis, text-indent, text-overflow, text-rendering, text-shadow, text-transform, text-underline-offset, text-wrap, top, touch-action, transform, transform-origin, transform-style, transition, transition-delay, transition-duration, transition-property, transition-timing-function, translate, user-select, vertical-align, view-transition-name, visibility, white-space, widows, width, will-change, word-break, word-spacing, writing-mode, z-index, zoom

# The value keywords: the words on the right of a colon that the language itself provides.
language.types = flex, grid, all, absolute, relative, fixed, sticky, static, block, inline, inline-block, inline-flex, inline-grid, flow-root, list-item, table-cell, table-row, table-column, table-caption, none, contents, auto, hidden, visible, scroll, clip, collapse, solid, dashed, dotted, double, groove, ridge, outset, bold, bolder, lighter, normal, italic, oblique, small-caps, uppercase, lowercase, capitalize, underline, overline, line-through, center, start, end, justify, space-between, space-around, space-evenly, stretch, baseline, first, last, safe, unsafe, middle, text-top, text-bottom, sub, super, wrap, nowrap, wrap-reverse, row, row-reverse, column, column-reverse, pre, pre-wrap, pre-line, break-word, break-all, keep-all, ellipsis, inherit, initial, unset, revert, revert-layer, currentColor, transparent, cover, border-box, content-box, padding-box, no-repeat, repeat-x, repeat-y, both, forwards, backwards, infinite, alternate, alternate-reverse, running, paused, linear, ease, ease-in, ease-out, ease-in-out, step-start, step-end, pointer, default, crosshair, move, grab, grabbing, not-allowed, wait, help, progress, zoom-in, zoom-out, col-resize, row-resize, ns-resize, ew-resize, nesw-resize, nwse-resize, portrait, landscape, print, screen, speech, dark, light, serif, sans-serif, monospace, cursive, fantasy, system-ui, ui-monospace, ui-serif, ui-sans-serif, min-content, max-content, fit-content, thin, medium, thick, small, large, larger, smaller, x-small, xx-small, x-large, xx-large, ltr, rtl, horizontal, vertical, horizontal-tb, vertical-rl, vertical-lr, manual, isolate, disc, circle, square, decimal, lower-alpha, upper-alpha, lower-roman, upper-roman, outside, inside, separate, always, avoid, recto, verso, crisp-edges, pixelated, multiply, overlay, darken, lighten, difference, exclusion, hue, saturation, luminosity, allow-end, force-end, balance, each-line, hanging, blink, wavy, under, over, srgb, display-p3, currentcolor, anywhere

# Dracula, which is what the other bundled plugins use, so a folder of mixed files is coloured with
# one scheme rather than four. It is Quill's per-token mapping rather than Dracula's CSS-specific one:
# `theme.builtin` is purple in every other manifest here, and a stylesheet in the tab beside a
# TypeScript file should be read in the same scheme rather than a second one that almost agrees.
theme.name     = Dracula
theme.keyword  = #FF79C6
theme.builtin  = #BD93F9
theme.function = #50FA7B
theme.type     = #8BE9FD
theme.string   = #F1FA8C
theme.number   = #FFB86C
theme.comment  = #6272A4
theme.operator = #FF79C6
theme.text     = #F8F8F2
